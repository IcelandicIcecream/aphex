// Storage adapter factory
import type { CMSConfig } from '../config.js';

export function createStorageAdapter(config: CMSConfig) {
  // This will be implemented when we need it
  // For now, adapters are created in the hooks
  return null;
}